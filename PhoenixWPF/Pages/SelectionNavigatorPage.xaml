<Page x:Class="PhoenixWPF.Pages.SelectionNavigatorPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:PhoenixWPF.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="20" d:DesignWidth="800"
      Title="SelectionNavigatorPage">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="30" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="30" />
        </Grid.ColumnDefinitions>

        <Button x:Name="Back" Content="&lt;&lt;"
                Click="BackButton_Click"
                Grid.Column="0" 
                HorizontalAlignment="Center" 
                VerticalAlignment="Center"
                IsEnabled="False"/>

        <TextBlock x:Name="Display" Text="" 
                   Grid.Column="1" 
                   Foreground="BlanchedAlmond"
                   HorizontalAlignment="Center" 
                   VerticalAlignment="Center"
                   FontSize="12" />

        <Button x:Name="Forward" Content="&gt;&gt;"
                Click="ForwardButton_Click"
                Grid.Column="2" 
                HorizontalAlignment="Center" 
                VerticalAlignment="Center"
                IsEnabled="False" />
    </Grid>
</Page>
